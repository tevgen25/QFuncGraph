# QFuncGraph - Графический построитель функций

## Описание программы

QFuncGraph - это десктопное приложение на C++/Qt для построения графиков математических функций. Программа позволяет пользователям вводить математические выражения и визуализировать их в виде графиков с настраиваемой областью определения.

## Основные возможности

- **Ввод математических функций** с поддержкой стандартных операций и функций
- **Настройка области определения** (диапазона по оси X)
- **Визуализация графиков** с помощью библиотеки QCustomPlot
- **Интерактивное управление графиком** - масштабирование, перемещение, выделение
- **Автоматическое масштабирование** под данные
- **Валидация ввода** с информативными сообщениями об ошибках

## Поддерживаемые математические функции

Программа использует библиотеку muParser, которая поддерживает:
- Базовые операции: `+`, `-`, `*`, `/`, `^` (возведение в степень)
- Тригонометрические функции: `sin(x)`, `cos(x)`, `tan(x)`
- Обратные тригонометрические: `asin(x)`, `acos(x)`, `atan(x)`
- Гиперболические: `sinh(x)`, `cosh(x)`, `tanh(x)`
- Логарифмические: `log(x)` (натуральный), `log10(x)` (десятичный)
- Экспоненциальные: `exp(x)`
- Другие: `sqrt(x)`, `abs(x)`

## Интерфейс пользователя

Приложение имеет интуитивно понятный интерфейс, разделенный на две основные области:

### Верхняя панель управления:
- **Поле ввода функции** - для ввода математического выражения
- **Поле области определения** - для задания диапазона значений X
- **Кнопки управления**:
  - "Рассчитать" - вычисляет значения функции
  - "Построить график" - отображает график

### Область графика:
- Интерактивный график с возможностями:
  - Перетаскивание (зажатая левая кнопка мыши)
  - Масштабирование (колесо мыши)
  - Выделение элементов
- Настраиваемые оси с фиксированным шагом сетки
- Белый фон для лучшей читаемости

## Примеры использования

### Простая квадратичная функция:
```
Функция: x^2 + 2*x + 1
Область определения: -5 5
```

### Тригонометрическая функция:
```
Функция: sin(x) * cos(2*x)
Область определения: -3.14 3.14
```

### Комплексное выражение:
```
Функция: exp(-x^2/2) * sin(10*x)
Область определения: -3 3
```

## Технические детали

### Стек технологий:
- **C++17** - основной язык программирования
- **Qt 5.14.2** - фреймворк для GUI
- **QCustomPlot** - библиотека для построения графиков
- **muParser** - библиотека для парсинга математических выражений
- **CMake** - система сборки

### Структура проекта:
```
QFuncGraph/
├── main.cpp              # Точка входа в приложение
├── mainwindow.h/cpp      # Основное окно приложения
├── qcustomplot.h/cpp     # Библиотека графиков
├── lib/muparser/         # Математический парсер
└── CMakeLists.txt        # Конфигурация сборки
```

## Сборка и запуск

### Требования:
- Компилятор C++ с поддержкой C++17
- Qt 5.14.2 (MinGW версия)
- CMake 3.10 или выше

### Сборка:
```bash
mkdir build
cd build
cmake ..
cmake --build . --config Release
```

### Запуск:
После сборки запустите `QFuncGraph.exe` из папки сборки.

## Использование

1. **Введите функцию** в первое текстовое поле (используйте 'x' как переменную)
2. **Задайте область определения** во втором поле (два числа через пробел: x_min x_max)
3. **Нажмите "Рассчитать"** для вычисления значений функции
4. **Нажмите "Построить график"** для отображения графика
5. **Взаимодействуйте с графиком** используя мышь для навигации

## Особенности реализации

- **Динамическое вычисление** - 500 точек на заданном интервале
- **Обработка ошибок** - информативные сообщения при неверном вводе
- **Оптимальное использование пространства** - пропорциональное распределение 20%/80%
- **Кроссплатформенность** - может быть собрано под Windows, Linux, macOS

## Лицензия

Проект использует компоненты с различными лицензиями:
- Qt - LGPL v3
- QCustomPlot - GPL v3
- muParser - BSD-style license

## Разработчик
Туманян Евгений Эдуардович

Приложение демонстрирует возможности:
- Создания GUI приложений на Qt
- Интеграции сторонних библиотек
- Обработки математических выражений
- Построения научной визуализации
